# ROADMAP — Local RAG System 待优化功能清单

> 记录时间：2026-07-21  
> 最后更新：2026-07-24  
> 持续维护，有新想法时直接追加。按模块分组，优先级仅供参考。

---

## 🧠 知识图谱优化

| # | 内容 | 状态 | 备注 |
|---|---|---|---|
| G1 | LLM 一体化实体+关系抽取（方向三） | ✅ 已完成 | `LlmOnlyBuilder` 已实现，支持 batch_size + concurrency，可在 graph_config.yaml 中切换 |
| G2 | 引入 LightRAG 框架（方向四-A） | 待开发 | 接口已预留（`LightRagBuilder`），需安装 `lightrag-hku`，检索路径也需适配 |
| G3 | 引入 Microsoft GraphRAG（方向四-B） | 待开发 | 接口已预留（`GraphRagBuilder`），适合大规模文档，需安装 `graphrag` |
| G4 | 图谱检索策略配置化 | 待开发 | 在配置文件（`.env` 或 `graph_config.yaml`）中支持：① 检索深度（`GRAPH_SEARCH_DEPTH`，当前硬编码为 1，支持调整为 N 跳）；② 图谱召回分片数量（`GRAPH_TOP_K`，控制图谱路径关联的 chunk 数上限） |
| G5 | 引入 Kuzu 图数据库存储图谱数据 | ✅ 已完成 | 已引入 Kuzu 嵌入式图数据库替换 NetworkX + GraphML，支持历史快照查询、版本切换、跨版本 diff |

---

## ✂️ 知识库切片优化

| # | 内容 | 状态 | 备注 |
|---|---|---|---|
| C1 | 减少"单个标题单独成一个 chunk"的问题 | 待优化 | 当前递归切片对只有标题无内容的段落处理不佳，标题应合并到下一段 |
| C2 | 图文混排技术方案实施与验证 | 待研究 | 含图片/表格的 PDF/DOCX 如何提取图文关联信息，可考虑多模态模型辅助 |

---

## 💬 对话与会话管理

| # | 内容 | 状态 | 备注 |
|---|---|---|---|
| S1 | 对话历史持久化 | ✅ 已完成 | 已将会话和消息持久化到后端 SQLite；消息带时间戳；历史会话侧边栏显示最近消息时间；本地 localStorage 数据一次性迁移到后端 |

---

## 📚 知识库管理

| # | 内容 | 状态 | 备注 |
|---|---|---|---|
| K1 | 引入知识库概念，多知识库管理 | 待开发 | 当前所有文档在同一个库，需要支持创建/切换/命名知识库 |
| K2 | 文件预览功能 | 待开发 | 在文件管理页支持预览 PDF / DOCX 原文 |
| K3 | 按知识库检索 | 待开发 | 问答时可选择在指定知识库内检索，而非全库检索 |

---

## 🖥️ 前端交互优化

| # | 内容 | 状态 | 备注 |
|---|---|---|---|
| U1 | 图谱弹窗子图字体大小控制重构 | ✅ 已完成 | 已按窗口档位设置固定默认字号（标准/全屏/小窗），去掉了不稳定的动态调整控件 |
| U2 | 整体 UI 设计风格优化（去 AI 味） | 待开发 | 使用 qiaomu-design skill 对全站设计风格进行审查与重设计，去除模板感和 AI 生成痕迹，提升视觉品质和细节工艺；覆盖排版、间距、颜色系统、组件细节和交互动效 |

---

## 🛠️ 运维与工程化

| # | 内容 | 状态 | 备注 |
|---|---|---|---|
| O1 | 服务启停统一脚本封装 | 待开发 | 当前前后端需分别手动启动，后续封装为统一的 `start.sh` / `stop.sh`，支持一键启停全部服务，可考虑 `supervisor` 或 `pm2` 做进程守护 |
| O2 | 后端日志统一输出到日志文件 | 待开发 | 当前日志直接打印到终端，后续引入 Python `logging` 统一配置，按日期滚动写入日志文件（如 `logs/backend.log`），便于排查问题和监控 |

---

## 🔐 安全与权限

| # | 内容 | 状态 | 备注 |
|---|---|---|---|
| A1 | 引入登录和权限控制 | 待开发 | 支持用户登录，知识库和会话按用户隔离 |

---

## 已完成的重要功能（供参考）

- ✅ 双色系主题（暗/亮）+ Inter 字体
- ✅ Markdown 渲染 + 引用编号点击定位
- ✅ 图谱增强检索（NER + 关键词双路）
- ✅ 来源 badge 标注（图谱 vs 向量）
- ✅ 图谱实体弹窗（关系子图，多档尺寸，版本标注）
- ✅ 知识图谱版本快照与 diff 对比（含历史版本子图查询）
- ✅ 检索调试页面（流式展示 + Prompt/Context 查看，命中节点可点击弹出子图）
- ✅ 图谱构建策略插件化（ner_llm / llm_only / lightrag / graphrag 可配置）
- ✅ 实体过滤规则配置化（graph_config.yaml）
- ✅ LLM 一体化实体+关系抽取（llm_only 策略）
- ✅ Kuzu 图数据库替换 NetworkX（快照/版本切换/历史查询）
- ✅ 对话历史后端持久化 + 消息时间线
- ✅ 图谱构建确认提示（展示策略/模型/文档清单）
- ✅ 图谱版本切换不被自动还原

